# exp0005_ensemble_light — コメントのみのスケルトン
# 目的: 軽量アンサンブル + 時間ガード（TTA/stride/候補N/解像度の自動切替）
# 入力: 各 fold の分類スコア（exp0003）
# 出力: fold アンサンブルの最終スコア（OOF, テスト）
# 注意: 実装は行わずコメントのみ。
#
# 方針（コメント）:
# - 重み付け: 各 fold を等重み or 温度スケーリング
# - 時間ガード: P100/T4 実測に合わせ切替閾値を校正（kaggle_infer.py 内のコメント）
# - 失敗時: 完走最優先で TTA→stride→候補N→解像度 の順に縮退
